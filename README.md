# Setup for Materialise CSS workflow

## Stuff included
- Material-css
- Scss
- Bourbon
- Browser-sync

## To install
- git clone
- `npm install` or `yarn install`
- dev mode `npm run dev` or `yarn run dev`

## Tested
- Mac with Yarn
